# ===== Python =====
__pycache__/
*.py[cod]

# 构建镜像时本地使用的临时 DOCKER_CONFIG（含 buildx 缓存）
.docker-test/

# 真实配置（含密码/token/绝对路径，勿提交）；模板见 .env.example / conf/config.toml.example
.env
conf/config.toml
conf/rclone.conf
docker-compose.yml

# 运行时生成的数据/日志（如部署在项目目录内时）
*.log
backup.log

# 编辑器/系统文件
.DS_Store
*.swp
*.swp
.idea/
.vscode/
